# Train or sample from a pixel based diffusion model
name: "pixel_diffusion_model"
type: pixel
testing: True
diffusion:
  type: pixel
  n_steps: 100 # T (100-400)
  n_samples: 16 #number of generated examples after training
  autoencoder: False
batch_size: 2
epochs: 5
learning_rate: 0.005
loss_fn: mse
model: 
  type: UNet
  params:
    channel_multipliers: [1, 2, 4, 8] #64, 128, 256, 512, etc.
    image_size: 8 #for image rescaling
    n_channels: 64 #first feature map output channels
    out_channels: 1 #noise prediction
    with_time_emb: True
data:
  dataset: "MNIST"
  image_channels: 1
